pseudocode for setenv

if userinput[0] is equal to setenv
call setenv function
if userinput[1] == NULL or userinput[2] == NULL
print error to stderr (Usage: setenv VARIABLE VALUE)
return (-1);
else
search through environ list for name, if found
set value to new value
return (0);
else if not found and environ[i + 1] == end of file
add variable and value to list end.

unsetenv
loop through environ list
if name is found
delete from the list
move subsequent item up by one

check if buffer contains ;
if yes strtok using ;
then while arr from string to k is not NULL call execute
